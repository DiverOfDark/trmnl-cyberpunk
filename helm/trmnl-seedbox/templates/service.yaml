apiVersion: v1
kind: Service
metadata:
  name: {{ include "trmnl-seedbox.fullname" . }}
  labels:
    {{- include "trmnl-seedbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trmnl-seedbox.selectorLabels" . | nindent 4 }}
